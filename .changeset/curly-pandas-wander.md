---
'@tylertech/forge-extended': minor
---

feat(theme-editor): add `forge-theme-editor` for authoring a complete Forge theme — edits all 101 `--forge-theme-*` tokens with filtering and per-token revert, derives a full palette from seed colors, reports the worst `on-*` contrast ratios, imports and exports JSON/Sass/CSS, and previews the theme on the live document
