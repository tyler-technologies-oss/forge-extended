---
'@tylertech/forge-extended': minor
---

feat(theme-editor): add `forge-theme-editor` for authoring a complete Forge theme — keeps light and dark as independent variants, edits all 101 `--forge-theme-*` tokens with filtering and per-token revert, derives a full palette from seed colors, renders each `on-*` contrast pair for real against its ratio, previews the theme on a panel of live Forge components in a dialog without touching the host page, stays readable while a page-wide preview is applied, and imports and exports JSON/Sass/CSS
