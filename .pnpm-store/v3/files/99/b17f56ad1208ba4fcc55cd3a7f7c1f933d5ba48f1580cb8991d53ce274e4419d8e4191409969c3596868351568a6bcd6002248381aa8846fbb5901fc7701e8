/**
 * @license
 * Copyright Tyler Technologies, Inc. 
 * License: Apache-2.0
 */
export declare class ColorPickerGradientSlider {
    private _rootElement;
    private _changeListener;
    private _xPercent;
    private _yPercent;
    private _thumbElement;
    private _keydownListener;
    private _downListener;
    private _moveListener;
    private _upListener;
    constructor(_rootElement: HTMLElement, _changeListener: (x: number, y: number) => void);
    destroy(): void;
    setValue(saturation: number, value: number): void;
    private _initialize;
    private _listen;
    private _unlisten;
    private _onKeydown;
    private _onDown;
    private _onMove;
    private _onUp;
    private _updateThumbPosition;
    private _calculateSliderPercent;
    private _setThumbPosition;
    private _notify;
}
