/**
 * @license
 * Copyright Tyler Technologies, Inc. 
 * License: Apache-2.0
 */
@use '../core/styles/typography';

@mixin host {
  /* Typography styles are set on the host to make it easier to override them via a class */
  @include typography.style(label1);

  display: inline-block;
  cursor: default;
}
