/**
 * @license
 * Copyright Tyler Technologies, Inc. 
 * License: Apache-2.0
 */
import { COMPONENT_NAME_PREFIX } from '../constants';
const elementName = `${COMPONENT_NAME_PREFIX}badge`;
const attributes = {
    DOT: 'dot',
    HIDE: 'hide',
    THEME: 'theme',
    STRONG: 'strong'
};
const classes = {
    OPEN: 'open'
};
const selectors = {
    ROOT: '.forge-badge'
};
const defaults = {
    THEME: 'default'
};
export const BADGE_CONSTANTS = {
    elementName,
    attributes,
    selectors,
    classes,
    defaults
};
