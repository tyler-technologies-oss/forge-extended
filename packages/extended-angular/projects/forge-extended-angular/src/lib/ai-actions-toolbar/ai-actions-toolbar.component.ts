// This code was generated by the angular-custom-elements-schematic.  Any changes will be overwritten next time it runs.
import {
  Component,
  ElementRef,
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  NgZone,
  Input,
  inject
} from '@angular/core';
import {
  AiActionsToolbarComponent as AiActionsToolbarComponentCustomElement,
  defineAiActionsToolbarComponent
} from '@tylertech/forge-extended';

/**  */
@Component({
  selector: 'forge-ai-actions-toolbar',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '<ng-content></ng-content>',
  standalone: false
})
export class AiActionsToolbarComponent {
  protected elementRef = inject<ElementRef<AiActionsToolbarComponentCustomElement>>(ElementRef);
  protected zone = inject(NgZone);

  /** The forge-ai-actions-toolbar element. */
  public readonly nativeElement = this.elementRef.nativeElement;

  constructor() {
    defineAiActionsToolbarComponent();
    const changeDetectorRef = inject(ChangeDetectorRef);
    changeDetectorRef.detach();
  }
}
