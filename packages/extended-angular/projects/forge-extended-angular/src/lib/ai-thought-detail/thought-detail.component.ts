// This code was generated by the angular-custom-elements-schematic.  Any changes will be overwritten next time it runs.
import {
  Component,
  ElementRef,
  ChangeDetectionStrategy,
  ChangeDetectorRef,
  NgZone,
  Input,
  inject
} from '@angular/core';
import {
  ThoughtDetailComponent as ThoughtDetailComponentCustomElement,
  defineThoughtDetailComponent
} from '@tylertech/forge-extended';

/**  */
@Component({
  selector: 'forge-ai-thought-detail',
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '<ng-content></ng-content>',
  standalone: false
})
export class ThoughtDetailComponent {
  protected elementRef = inject<ElementRef<ThoughtDetailComponentCustomElement>>(ElementRef);
  protected zone = inject(NgZone);

  /** The forge-ai-thought-detail element. */
  public readonly nativeElement = this.elementRef.nativeElement;

  constructor() {
    defineThoughtDetailComponent();
    const changeDetectorRef = inject(ChangeDetectorRef);
    changeDetectorRef.detach();
  }
}
