import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';
import CustomArgTypes from '../../../../blocks/CustomArgTypes';
import * as AiDialogStories from './AiDialog.stories';

<Meta of={AiDialogStories} />

<Title />

The AI Dialog provides a modal dialog container optimized for AI chat interfaces. It automatically switches between modal and fullscreen modes based on viewport size.

<Canvas of={AiDialogStories.Demo} />

## API

<CustomArgTypes />
