import { Meta, Title, Canvas } from '@storybook/addon-docs/blocks';
import CustomArgTypes from '../../blocks/CustomArgTypes';
import * as StructuredCardStories from './StructuredCard.stories';
import * as WithTableStories from './WithTable.stories';
import * as WithBeforeTitleSlotStories from './WithBeforeTitleSlot.stories';
import * as ScrollableBodyContentStories from './ScrollableBodyContent.stories';
import * as WithHeaderActionsStories from './WithHeaderActions.stories';
import * as CardWithNoHeaderStories from './CardWithNoHeader.stories';
import * as CardWithNoFooterStories from './CardWithNoFooter.stories';

<Meta of={StructuredCardStories} />

<Title />

🚧 **Experimental** 🚧

A pre-configured card layout optimized for common patterns in Tyler Cloud applications. Using a slot-based architecture, it provides designated areas for titles, actions, body content, and footer buttons—helping developers build consistent card UIs without manually configuring the same layout structure repeatedly. Ideal for table containers and self-contained content sections that require their own actions.

<Canvas of={StructuredCardStories.Demo} />

## Slots

- **before-title** - Content before the title (e.g., back button, icon)
- **title** - Card title/heading
- **header-actions** - Actions in the header (e.g., menus, buttons)
- **body** - Main content area (expands to fill space)
- **footer-start** - Footer start content (e.g., pagination)
- **footer-secondary-action** - Secondary action button
- **footer-primary-action** - Primary action button

> **Note:** Parent container must have a defined height for the card to fill available space.

## Examples

#### With Header Actions

Add menus, buttons, or controls to the header using the `header-actions` slot.

<Canvas of={WithHeaderActionsStories.WithHeaderActions} />

#### Detail Panel with Back Navigation

Use the `before-title` slot for back buttons or navigation icons.

<Canvas of={WithBeforeTitleSlotStories.WithBeforeTitleSlot} />

#### Data Table with Pagination

Set `with-table` when the body contains a table. This removes body padding, sets gap to `0`, and removes footer bottom padding for edge-to-edge table layouts.

<Canvas of={WithTableStories.WithTable} />

#### Scrollable Content

Set `--forge-structured-card-body-height` to a fixed value to enable scrollable body content.

<Canvas of={ScrollableBodyContentStories.ScrollableBodyContent} />

#### Card Without Header

Simple cards that don't need a header section.

<Canvas of={CardWithNoHeaderStories.CardWithNoHeader} />

#### Card Without Footer

Cards that don't require footer actions.

<Canvas of={CardWithNoFooterStories.CardWithNoFooter} />

## API

<CustomArgTypes />
