import { type Meta, type StoryObj } from '@storybook/web-components';
import { html } from 'lit';

import '$lib/{{dashCase name}}';

const component = 'forge-{{dashCase name}}';

const meta = {
title: 'Components/{{titleCase (sentenceCase name)}}',
  render: args => {
    return html`<forge-{{dashCase name}}></forge-{{dashCase name}}>`;
  },
  component,
  argTypes: {

  },
  args: {
    
  }
} satisfies Meta;

export default meta;

type Story = StoryObj;

export const Demo: Story = {};
